import React from "react";

const Loader = () => {
  return (
    <div className="loader-ak"></div>
  );
};

export default Loader;

// Agrega este CSS en tu archivo global o en el componente con styled-jsx o similar:

/*

*/