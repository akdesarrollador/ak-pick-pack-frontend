enum Routes {
  Login = "/login",
  SearchOrders = "/busqueda-ordenes",
  PendingOrders = "/ordenes-pendientes",
  PendingProducts = "/productos-pendientes",
  Producto = "/producto",
  OrderPending = "/pedido",
  FinalOrder = "/finalizar-pedido",
}

export default Routes;
